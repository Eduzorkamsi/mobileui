import React from 'react'
import MenuBar from '../components/menuBar'

function Account () {
    return (
        <div>
            <text>Account</text>
            <MenuBar/>
        </div>
    );
};

export default Account;